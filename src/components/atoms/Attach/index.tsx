import { FC } from 'react'

import './styles.scss'

import attach from '../../../assets/images/attach.svg'

const Attach: FC = () => {
	return <img className='attach' src={attach} />
}

export default Attach
